# ftp-socket
A simple FTP-like file transfer system implemented in C using TCP sockets, supporting concurrent clients through process forking. This project demonstrates low-level network programming, socket management, and basic FTP command handling.
